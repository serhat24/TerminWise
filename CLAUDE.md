Read ROADMAP.md before doing anything. It is the single source of truth.

- Current phase: 1
- We work strictly phase by phase. Never start work belonging to a later
  phase. A phase is done only when its acceptance criteria pass.
- ADR-first: draft the ADR in docs/adr/ and get my approval BEFORE
  writing the implementing code.
- For Angular 22 (Signal Forms, zoneless), Microsoft Agent Framework and
  Azure AI Foundry: do NOT rely on training data — fetch current official
  docs first (these APIs changed recently).
- Update "Current phase" here when I confirm a phase is complete.
